"use client";

const Theme = () => {
  return (
    <span className="inline-flex w-10 h-10 rounded-full bg-neutral-200 items-center justify-center">
      {/* Light mode only */}
    </span>
  );
};

export default Theme;
